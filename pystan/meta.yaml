package:
  name: pystan
  version: !!str 2.5.0.0

source:
  fn: pystan-2.5.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pystan/pystan-2.5.0.0.tar.gz
  md5: ab1109c59eaeed80c21448257bededdc

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - cython

  run:
    - python
    - numpy
    - cython

test:
  imports:
    - pystan

  requires:
    - nose
    - matplotlib
    - scipy

  commands:
    - nosetests --exe pystan.tests.test_basic

about:
  home: https://github.com/stan-dev/pystan
  license: GNU General Public License
