package:
  name: testfixtures
  version: 3.0.2

source:
  fn: testfixtures-3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/testfixtures/testfixtures-3.0.2.tar.gz
  md5: b3e8281227b147804bf7c581c260b762

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - testfixtures

about:
  home: http://www.simplistix.co.uk/software/python/testfixtures
  license: MIT
